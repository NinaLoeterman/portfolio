import React from "react";

const useSidebar = () => {
  const text = {
    about: "about",
    projects: "projects",
    resume: "resume",
    contact: "contact",
  };

  return { text };
};

export default useSidebar;
